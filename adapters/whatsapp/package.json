{
  "name": "uniconnect-whatsapp",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "description": "WhatsApp worker for UniConnect AI: reads the group, answers when asked",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file=.env index.js",
    "groups": "node --env-file=.env groups.js",
    "check": "node --check index.js && node --check api.js && node --check groups.js && node --check avatar.js",
    "avatar": "node --env-file=.env avatar.js"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^7.0.0-rc14",
    "qrcode-terminal": "^0.12.0"
  }
}
